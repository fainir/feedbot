[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=3072' npm run build"]

[start]
cmd = "node .next/standalone/server.js"
